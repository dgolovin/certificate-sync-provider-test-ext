{
  "name": "certificate-sync-provider-test",
  "displayName": "Certificate Sync Provider Test",
  "description": "Test extension to verify certificate sync trust model for external extensions",
  "version": "0.0.1",
  "type": "module",
  "icon": "icon.png",
  "publisher": "test",
  "license": "Apache-2.0",
  "engines": {
    "podman-desktop": "^1.10.0"
  },
  "main": "./dist/extension.cjs",
  "contributes": {},
  "scripts": {
    "build": "vite build",
    "watch": "vite build -w",
    "test": "vitest run --coverage"
  },
  "dependencies": {
    "@podman-desktop/api": "^1.14.1"
  },
  "devDependencies": {
    "@types/node": "^22",
    "@vitest/coverage-v8": "^2.0.0",
    "typescript": "^5.5.0",
    "vite": "^6.0.0",
    "vitest": "^2.0.0"
  }
}
